enum Page {
    MENU,
    PLAYING,
    SETTINGS
}

enum Theme {
    Dark="Dark",
    Light="Light",
    System="System"
}

enum Cache {
    On="On",
    Off="Off"
}

enum SettingType {
SOUND,
MUSIC,
THEME
}

export {Page, Theme, SettingType, Cache};